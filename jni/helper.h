#include <cstdlib>
#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <algorithm>

inline bool space(char c);
inline bool notspace(char c);
std::vector<std::string> split(const std::string& s);
